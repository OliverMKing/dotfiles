# Dotfiles

This repository is a collection of scripts and configs to easily replicate my local Ubuntu Linux setup. The install script is idempotent so it can be used to upgrade environments to parity.

## Installation

```bash
chmod +x install.sh
./install.sh
```